# Puzzle Buddy icon options

Generated by `node Scripts/render-icon-options.mjs`.

## Selected (shipped)

**Current (Jun 2026):** buddy two-piece dark icon — applied manually from `../potential-icons/14-selected-buddy-dark.png` (same graphic for AppIcon + LaunchCrestHero). Rescaled to true full-bleed: puzzle cropped to its bounding box and centered at ~80% on a solid `#0a0d12` square (no baked-in rounded corners or black padding — iOS applies the corner mask).

| Asset | Source | Notes |
|-------|--------|-------|
| **AppIcon** | `14-selected-buddy-dark.png` | Two interlocking pieces, teal + cyan, `#0a0d12` full bleed |
| **LaunchCrestHero** | light: `15-buddy-light-launch.png` · dark: `14-selected-buddy-dark.png` | Light = buddy on `#f2f7fa` (matches launch/splash bg; puzzle blanks filled). Dark = full-bleed navy tile. |

**Previous (scene pipeline):** option 02 AppIcon + option 01 LaunchCrestHero — restore with `node Scripts/apply-selected-icons.mjs` (overwrites the buddy icon).

Size previews (simulated home screen mask + BrandMark clip): `previews/`

## Explorations

| File | Description |
|------|-------------|
| `00-current-shipped.png` | Previous restored blue crest (line art) |
| `01-teal-stage-assembled.png` | Teal jigsaw — assembled on light stage |
| `02-teal-dark-ring.png` | Teal jigsaw — App Store dark ring (exploration; inner roundrect omitted in production for iOS 26 glass) |
| `03-ocean-assembled.png` | Ocean blue jigsaw — assembled |
| `04-ocean-dark-ring.png` | Ocean blue jigsaw — dark ring |
| `05-teal-mid-assemble.png` | Teal jigsaw — mid assembly (dynamic) |
| `06-teal-brand-gradient.png` | Teal jigsaw — brand gradient background |

Regenerate explorations: `node Scripts/render-icon-options.mjs`  
Re-apply production assets: `node Scripts/apply-selected-icons.mjs`

For full Liquid Glass layered icons later, export flat background + puzzle layers from `puzzle-scene.jsx` into Apple Icon Composer.
