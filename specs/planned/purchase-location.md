# Purchase location

**Target:** 1.2.0

## Scope

- Field: `purchaseLocation: String?` (free text or picker with favorites)
- Form + detail + list search
- Filter chip on collection list
- Stats: “Top stores” section
- IPDb CSV column mapping if present in export

## Examples

Thrift store name, Amazon, Barnes & Noble, puzzle swap group
