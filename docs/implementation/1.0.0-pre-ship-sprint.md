# 1.0.0 pre-ship sprint (metadata & stats)

**Target:** inaugural **1.0.0** App Store release (local-only; import/export stays gated)  
**Status:** Complete (dev) — not yet submitted  
**Last updated:** 2026-06-27

Internal sprint name only; this work ships as part of **1.0.0**, not a separate public release.

## Phases

| Phase | Theme | Status |
|-------|--------|--------|
| 0 | Enums & conventions | [x] |
| 1 | Abandoned status + start-date picker | [x] |
| 2 | Stats: wishlist + avg days | [x] |
| 3 | Milestones | [x] |
| 4 | Pick-next on Stats tab | [x] |
| 5 | Metadata schema batch | [x] |
| 6 | Metadata form + detail | [x] |
| 7 | Metadata filters + stats | [x] |
| 8 | Tests, docs, version 1.0.0 | [x] |

## Phase 0 — Enums

New file `PuzzleMetadataEnums.swift`: `PuzzleType`, `PuzzleMaterial`, `PuzzleDisposition`.

## Phase 1 — Abandoned + start date

- `Puzzle.Status.abandoned`
- List tab, pill, date semantics, pick-next exclusion, IPDb folder
- Manual start-date picker on form (In-Progress / Completed)

## Phase 2 — Stats

- `wishlistCount`, `abandonedCount`, `averageDaysToComplete`
- Cards on `CollectionStatsView`

## Phase 3 — Milestones

- `CollectionMilestones.swift` + UserDefaults acknowledge
- Banner on Stats tab

## Phase 4 — Pick-next on Stats

- Dice toolbar → `PickNextPuzzleView` sheet

## Phase 5 — Schema

Fields: `purchaseLocation`, `releaseYear`, `puzzleType`, `material`, `disposition`  
Wire: `PuzzleRecord`, export, IPDb columns.

## Phase 6 — UI

Form sections + detail rows. Rename source label → Brand.

## Phase 7 — Filters + stats

Search extension; type/material/disposition filters; favorite type, top stores.

## Phase 8 — Ship prep

Tests, `feature-inventory.md`, backlog ✅ marks; **`MARKETING_VERSION` 1.0.0**.

## Out of scope (superseded by expanded sprint)

Import/export UI, JSON restore, timer, multi-photo, widget, auth, localization — see [`1.0.0-expanded-feature-sprint.md`](1.0.0-expanded-feature-sprint.md) for what moved into 1.0.

## Definition of done

- [x] Unit tests green (180)
- [x] `feature-inventory.md` updated
- [x] Version **1.0.0** (build 2)
